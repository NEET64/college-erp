<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>University ERP</title>
  </head>
  <body>
    <h1>All Courses</h1>
    <form action="/course/new" method="GET">
      <button>New Course</button>
    </form>
    <ul>
      <% for(let course of courses) { %>
      <li><a href="/course/<%=course._id%>"><%= course.name %></a></li>
      <% } %>
    </ul>
  </body>
</html>
